<?php
session_start();

// Check if user is logged in
if(!isset($_SESSION["loggedin"]) || $_SESSION["loggedin"] !== true){
    header("location: login.php");
    exit;
}

// Include database connection
require_once "config.php";

// Retrieve user's schedules
$stmt = $mysqli->prepare("SELECT start_date, end_date, num_plants, times_per_week FROM schedules WHERE user_id = ?");
$stmt->bind_param("i", $user_id);
$user_id = $_SESSION["id"];
$stmt->execute();
$result = $stmt->get_result();
?>

<!DOCTYPE html>
<html>
<head>
    <title>Schedule Plant Watering</title>
</head>
<body>
    <h1>Schedule Plant Watering</h1>
    <p>Fill out the form below to schedule days for plant watering. Please expect that a matching plant waterer will contact you shortly.</p>

    <form method="post" action="<?php echo htmlspecialchars($_SERVER["PHP_SELF"]); ?>">
        <label>Start Date:</label>
        <input type="date" name="start_date" required><br><br>

        <label>End Date:</label>
        <input type="date" name="end_date" required><br><br>

        <label>Number of Plants:</label>
        <input type="number" name="num_plants" min="1" required><br><br>

        <label>Times per Week:</label>
        <input type="number" name="times_per_week" min="1" required><br><br>

        <input type="submit" value="Submit">
    </form>

    <h2>My Schedule</h2>
    <table>
        <tr>
            <th>Start Date</th>
            <th>End Date</th>
            <th>Number of Plants</th>
            <th>Times per Week</th>
        </tr>
        <?php while($row = $result->fetch_assoc()) { ?>
            <tr>
                <td><?php echo $row["start_date"]; ?></td>
                <td><?php echo $row["end_date"]; ?></td>
                <td><?php echo $row["num_plants"]; ?></td>
                <td><?php echo $row["times_per_week"]; ?></td>
            </tr>
        <?php } ?>
    </table>
</body>
</html>
